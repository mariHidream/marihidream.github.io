import React from 'react';
import transition from '../transition';


const Home = () => {
    return (
        <div>
            <h2>Home</h2>
        </div>
    );
};

export default transition(Home);