import React from 'react';
import transition from '../transition';

const About = () => {
    return (
        <div>
            <h2>About</h2>
        </div>
    );
};

export default transition(About);